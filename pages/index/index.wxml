<!--index.wxml-->

<view class="container">

  <view class="userinfo" bindtap="ChangeNickname">
    <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
    <text class="userinfo-nickname">{{userInfo.nickName}}</text>
  </view>
  <view wx:if="{{is_use == 2}}">
  <view class="usermotto">
  <text>最后有效时间：{{ current_user.end_time }}</text>
  </view>
  <view class="usermotto">
    <button bindtap="pay" type="primary">续费使用</button>
  </view>
  </view>
  <view wx:elif="{{is_use == 0}}">
  <view class="usermotto">
    <text>网络连接不畅...</text>
  </view>
  <view class="usermotto" bindtap="reconnect">
    <button>加速连接</button>
  </view>
  </view>
  <view wx:elif="{{is_use == 1}}">
     <navigator url="../todos/todos" open-type='switchTab'>开始“希望”</navigator>
  </view>
</view>
